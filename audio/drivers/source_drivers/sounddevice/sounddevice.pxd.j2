from runner_utils cimport times_t
from source_drivers cimport source_driver

cdef class SounddeviceSourceDriver(source_driver.SourceDriver):
    cdef object stream
    cdef float sleep_duration

    cdef size_t run(
        self, times_t *times, void *inBuf, size_t packetSize,
        object out_sigs
    ) except *
    cdef void exit_handler(self, int exitStatus) except *
